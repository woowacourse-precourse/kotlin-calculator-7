package calculator

class Calculator {
    fun calculateString(inputString: String) {
        // 1. 커스텀 구분자가 있다면 커스텀 구분자를 추가하고, 문자열을 분리한다. divide
        // 2. 구분자를 기준으로 각 숫자들을 분리한다. split
        // 3. 각 숫자들의 총 합을 구한다.
        // 4. 결과를 출력한다.
    }

    companion object {
        private val validator = Validator()
    }
}
